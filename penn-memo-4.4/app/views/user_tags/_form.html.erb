
<div class="container" style="width:200px">

<h1>Add Tag</h1>

<br>


<%= form_for(user_tag) do |f| %>
  <% if user_tag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_tag.errors.count, "error") %> prohibited this user_tag from being saved:</h2>

      <ul>
      <% user_tag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.hidden_field :user_id, :value => current_user.id, class:"form-control"  %>
  </div>

  <div class="form-group">
    <%= f.label :tag_id %>
    #&nbsp;<%= collection_select :user_tag, :tag_id, Tag.all, :id, :name ,class:"form-control"%>
  </div>

  <div class="actions">
    <%= f.submit "Add", class:"btn btn-default ", style:"width:138px"%>
  </div>

    <%= link_to 'Cancel', memos_path, class:"btn btn-default", style:"width:138px"%>


<% end %>



</div>
